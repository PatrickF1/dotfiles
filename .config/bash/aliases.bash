#!/usr/bin/env bash
alias ls="ls -GTh"
alias mkdir="mkdir -p"
alias grep="grep --color=auto"
alias history="history | tail -r | less"
alias ..="cd .."
alias vim="nvim"
alias clearf="pbpaste | pbcopy"
alias git="hub"
