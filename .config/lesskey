# less automatically loads ~/.config/lesskey to configure itself, no need to set environment variable
#command
t   toggle-option --ignore-case\n
# adjust forw/back-line to move faster, also affects mouse wheel
\ku back-line 5
\kd forw-line 5
# search for the symbols that delta sets in git patches to delineate files and hunks
D   forw-search \^(Δ|•)\n

#env
# $ is necessary to delinate some options
LESS = --quit-on-intr --ignore-case --RAW-CONTROL-CHARS --tabs=4 --window=-4 --color=d+b$ --color=u+g$ --color=s+y$ --prompt=sInput= %f, Lines= %lt-%lb / %L$
# don't store any history of commands executed in less
LESSHISTFILE = /dev/null
